/**
 * EduAttend — Admin panel (optional UI helpers only).
 * Attendance, QR, students, and reports use pure PHP forms — no API calls required.
 */
document.addEventListener('DOMContentLoaded', function () {
  // Reserved for non-critical UI (e.g. confirm dialogs) if needed later.
});
